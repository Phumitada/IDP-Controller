apiVersion: paas.internal/v1
kind: Database
metadata:
  labels:
    app.kubernetes.io/name: idp-controller
    app.kubernetes.io/managed-by: kustomize
  name: database-sample
spec:
  # TODO(user): Add fields here
